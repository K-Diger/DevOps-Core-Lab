apiVersion: apps/v1
kind: Deployment
metadata:
  name: tlm-api
  namespace: mis-dev
  labels:
    app: tlm-api
    app.kubernetes.io/name: tlm-api
    app.kubernetes.io/part-of: mis-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tlm-api
  template:
    metadata:
      labels:
        app: tlm-api
        app.kubernetes.io/name: tlm-api
    spec:
      imagePullSecrets:
        - name: harbor-pull-secret
      containers:
        - name: tlm-api
          image: dev-mis-registry.smiledev.net/ep-be-tlm/dev-tlm-api:latest
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: dev
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector.monitoring.svc:4318
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: http/protobuf
            - name: OTEL_SDK_DISABLED
              value: "false"
            - name: OTEL_SERVICE_NAME
              value: ep-be-tlm-api
            - name: OTEL_METRICS_ENABLED
              value: "true"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.namespace=mis-dev,deployment.environment=development"
          resources:
            requests:
              cpu: 250m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 4Gi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: tlm-api
  namespace: mis-dev
  labels:
    app: tlm-api
spec:
  selector:
    app: tlm-api
  ports:
    - port: 8080
      targetPort: 8080
      name: http
